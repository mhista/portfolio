name: port
description: A new Jaspr project
version: 0.0.1

environment:
  sdk: ^3.10.0

dependencies:
  jaspr: ^0.22.0
  jaspr_router: ^0.8.1
  mailer: ^6.6.0
  relic: ^0.14.0
  # shelf: ^1.4.2
  # shelf_router: ^1.1.4

dev_dependencies:
  build_runner: ^2.10.0
  build_web_compilers: ^4.4.6
  jaspr_builder: ^0.22.0
  lints: ^5.0.0
  jaspr_tailwind: ^0.3.5

jaspr:
  mode: server
